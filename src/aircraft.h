#pragma once

class Aircraft
{
public:
  double latitude = 0.0;        // rad
  double longitude = 0.0;       // rad
  double altitude = 0.0;        // m
  double velocityBodyX = 0.0;   // m/s (North)
  double velocityBodyY = 0.0;   // m/s (East)
  double velocityBodyZ = 0.0;   // m/s (Down)

private:

};
